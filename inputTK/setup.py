from setuptools import setup

setup(
   name='inputTK',
   version='1.0',
   description=' useful module',
   author='Edwin Saul',
   author_email='edwinsaulpm@gmail.com',
   packages=['inputTK'],  #same as name
   #install_requires=[''], #external packages as dependencies
)
